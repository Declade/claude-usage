---
phase: 01-scaffold
plan: 01
subsystem: infra
tags: [xcodegen, swiftui, menubarextra, macos, swift6, entitlements, lsuielement]

requires: []
provides:
  - XcodeGen-generated ClaudeUsage.xcodeproj (reproducible, git-friendly)
  - MenuBarExtra(.window) app entry point with no Dock icon (LSUIElement=true)
  - App Sandbox disabled via entitlements (required for ~/.claude/ file reads)
  - Quit button in menu panel (satisfies DISP-05)
  - Verification script for LSUIElement and sandbox checks (scripts/verify-scaffold.sh)
  - Empty test target placeholder (ClaudeUsageTests/PlaceholderTests.swift)
affects: [02-data, 03-ui, 04-polish]

tech-stack:
  added: [XcodeGen 2.45.3, SwiftUI MenuBarExtra, Swift 6.0, macOS 14.0 deployment target]
  patterns:
    - project.yml as source of truth for Xcode project (regenerate after structural changes)
    - MenuBarExtra(.window) scene as sole app entry — no WindowGroup, no AppDelegate
    - App Sandbox disabled in entitlements.properties (not build settings) for local file reads

key-files:
  created:
    - project.yml
    - ClaudeUsage/ClaudeUsageApp.swift
    - ClaudeUsage/ContentView.swift
    - ClaudeUsage/Assets.xcassets/.gitkeep
    - ClaudeUsage/Info.plist
    - ClaudeUsage/ClaudeUsage.entitlements
    - ClaudeUsage.xcodeproj/project.pbxproj
    - ClaudeUsageTests/PlaceholderTests.swift
    - scripts/verify-scaffold.sh
  modified: []

key-decisions:
  - "macOS 14.0 deployment target — enables @Observable throughout codebase, covers ~90%+ of developer machines"
  - "XcodeGen over Xcode GUI — reproducible project.yml avoids xcodeproj binary merge conflicts"
  - "MenuBarExtra(.window) style — required for ProgressView and custom SwiftUI layouts in later phases"
  - "App Sandbox disabled via entitlements.properties — sandbox blocks ~/.claude/ reads in non-debug builds silently"

patterns-established:
  - "Pattern: XcodeGen regeneration — run xcodegen generate from project root after any target/settings change"
  - "Pattern: MenuBarExtra content width — ContentView wrapped with .frame(width: 320) in the scene closure"
  - "Pattern: Quit — NSApplication.shared.terminate(nil) from Button inside .window style panel"

requirements-completed: [MBAR-01, DISP-05]

duration: 2min
completed: 2026-03-11
---

# Phase 1 Plan 1: Scaffold Summary

**XcodeGen-generated macOS menu bar app with LSUIElement=true, sandbox disabled, and MenuBarExtra(.window) scene — buildable Xcode project locked in before any business logic**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-11T20:23:40Z
- **Completed:** 2026-03-11T20:26:25Z
- **Tasks:** 2 of 3 complete (Task 3 is human-verify checkpoint — awaiting user)
- **Files modified:** 9

## Accomplishments
- Generated reproducible ClaudeUsage.xcodeproj from project.yml via XcodeGen 2.45.3
- BUILD SUCCEEDED with Swift 6.0, macOS 14.0 deployment target
- LSUIElement=1 confirmed in built bundle (no Dock icon, no App Switcher)
- App Sandbox confirmed disabled ([Bool] false in entitlements) — file reads to ~/.claude/ will work
- MenuBarExtra(.window) scene established as sole UI entry point
- Quit button wired to NSApplication.shared.terminate(nil) (satisfies DISP-05)
- Verification script passes 2/2 checks automatically

## Task Commits

Each task was committed atomically:

1. **Task 1: Create verification infrastructure (Wave 0)** - `403362f` (chore)
2. **Task 2: Generate Xcode project from XcodeGen spec** - `cbb9b5f` (feat)
3. **Task 3: Verify running app** - awaiting human checkpoint approval

## Files Created/Modified
- `project.yml` - XcodeGen spec; source of truth for the Xcode project
- `ClaudeUsage/ClaudeUsageApp.swift` - @main entry with MenuBarExtra("Claude Usage") + .menuBarExtraStyle(.window)
- `ClaudeUsage/ContentView.swift` - Stub panel with "Claude Usage" header and Quit button
- `ClaudeUsage/Assets.xcassets/.gitkeep` - Directory stub required by XcodeGen before project generation
- `ClaudeUsage/Info.plist` - Generated by XcodeGen; contains LSUIElement=true
- `ClaudeUsage/ClaudeUsage.entitlements` - Generated by XcodeGen; com.apple.security.app-sandbox=false
- `ClaudeUsage.xcodeproj/project.pbxproj` - Generated Xcode project file
- `ClaudeUsageTests/PlaceholderTests.swift` - Empty XCTestCase to satisfy test target linkage
- `scripts/verify-scaffold.sh` - Shell script: checks LSUIElement=1 and sandbox=false on built .app

## Decisions Made
- macOS 14.0 deployment target: enables @Observable throughout all phases; ~90%+ of developer machines covered
- XcodeGen for project creation: reproducible, CLI-driven, avoids xcodeproj binary merge conflicts
- MenuBarExtra(.window) style locked in Phase 1: cannot be changed per-view later; required for ProgressView in Phase 3
- App Sandbox disabled in entitlements.properties (not build settings): XcodeGen generates correct entitlements file; build-setting-only approach has signing-order bugs

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed Xcode plugin load failure before build**
- **Found during:** Task 2 (Xcode build step)
- **Issue:** xcodebuild exited with code 70 — IDESimulatorFoundation plugin failed to load (DVTDownloads framework symbol mismatch on macOS 26 + Xcode 26.3)
- **Fix:** Ran `xcodebuild -runFirstLaunch` (no sudo required) — installed required components, cleared symbol mismatch
- **Files modified:** None (system-level fix)
- **Verification:** Subsequent build exited 0 with BUILD SUCCEEDED
- **Committed in:** cbb9b5f (part of Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Fix was a system-level Xcode initialization step, not a code change. No scope creep.

## Issues Encountered
- xcodebuild -runFirstLaunch was required on this machine (Xcode 26.3 / macOS 26.3.1) before first build. Added to documentation as known environment quirk.

## User Setup Required
None — no external service configuration required. App is fully local.

## Next Phase Readiness
- Xcode scaffold is complete and buildable
- Architectural foundations locked: MenuBarExtra(.window), sandbox-off, LSUIElement=true, macOS 14.0, Swift 6.0
- Phase 2 (data layer) can begin immediately after Task 3 checkpoint approval
- Task 3 human verification still required: launch app, confirm no Dock icon, confirm panel opens, confirm Quit works

---
*Phase: 01-scaffold*
*Completed: 2026-03-11*
